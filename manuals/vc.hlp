    Описание дисковой резидентной оболочки The Vector Commander V2.0.
  После первого запуска VC (только если с дисковода!),необходимо скопировать
на диск С: файлы VC.COM и VC.EXT,для оставления его резидентным.
≥²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²▀
┼  Для выбора команд задействованных под клавишами F6-F10 необходимо нажать┼
┼клавишу <СС>,которая модифицирует клавиши F1-F5.                          ┼
≈■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■├
┼                      Управляющие клавиши.                                ┼
┼ <СТР>   - выделение/отмена файла в группу.                               ┼
┼ <УС+М>  - выделение/отмена файлов в группу по маске.Если ввести "пустую" ┼
┼           маску (сразу нажать <ВК>),то отменяется выбор всех файлов.     ┼
┼ <УС+О>  - показ экрана ОС,выход - любая клавиша.                         ┼
┼ <УС+F2> - смена диска.                                                   ┼
┼ <УС+F6> - перемещение группы файлов в другой User.                       ┼
┼ <УС+ВК> - помещение выделенного файла в командную строку.                ┼
┼  <АР2>  - выход из любого режима,или стирание командной строки.          ┼
┼  <ЗБ>   - стирание одного символа в командной строке.                    ┼
┼  <ВК>   - запуск командной строки на выполнение,если она пустая,то дей-  ┼
┼           ствие над выделенным файлом выполняется в зависимости от содер-┼
┼           жимого файла VC.EXT.                                           ┼
┼  <ТАБ>  - смена активной панели.                                         ┼
┼                                                                          ┼
≤²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²²▄

				*  *  *

		      Описание формата файла VC.EXT.
  При нажатии клавиши <ВК> (если командная строка пуста) действия VC над выде
ленным файлом зависят от расширения файла и от содержимого файла VC.EXT.
  В файле VC.EXT содержатся расширения (типы) файлов и производимые над ними
действиями.
  Первые три символа это расширение файла (можно использовать джокеры ? и *),
а вс╠ что находится после символа ":",является передаваемой ОС командной стро
кой.При ч╠м можно применять следующие специальные строки.

  !.!	-	означает имя и расширение файла.
  !	-	только имя.
  !:	-	текущий диск.
  !!	-	символ "!".

ПРИМЕР:

MAC:M80 =!

  Если расширение выделенного файла MAC,то командная строка переданная ОС бу-
дет такой:

M80 =NAME	,-где NAME имя выделенного файла.

  Если расширение выделенного файла не совпадает ни с одним расширением в фай
ле VC.EXT,то ни каких действий над ним произведено не будет.
  Файл  VC.EXT должен находится на диске C:,он представляет собой обычный тек
стовый файл,который можно редактировать.

				*  *  *

			  Описание команды VIEW.

			Управляющие клавиши:
  <ВВЕРХ>	- на страницу вверх.
  <ВНИЗ>	- на страницу вниз.
  <СС+ВВЕРХ>	- на строку вверх.
  <СС+ВНИЗ>	- на строку вниз.
  <ВПРАВО>	- сдвинуть просматриваемый файл на 32 символа вправо.
  <ВЛЕВО>	- 	" - . - "   влево.
  <F1>		- переключение кодировок текста с КОИ-8М на АЛЬТ и обратно.
  <АР2>		- выход.
  <ВК>		- задание строки символов для поиска в файле.
  <СС+СТР>	- поиск заданной строки от начала файла.
  <СТР>		- продолжение поиска заданной строки.


